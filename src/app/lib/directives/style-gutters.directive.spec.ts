import { StyleGuttersDirective } from './style-gutters.directive';

describe('StyleGuttersDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleGuttersDirective();
    expect(directive).toBeTruthy();
  });
});
