import { DisableGuttersDirective } from './disable-gutters.directive';

describe('DisableGuttersDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableGuttersDirective();
    expect(directive).toBeTruthy();
  });
});
